1、接口 interface
    主要用来描述类具有什么功能，而并不给出每个功能的具体实现

    让规范和实现分离，松耦合
    interface这一关键字产生一个完全抽象的类

2、一个类可以实现一个或多个接口

3、接口不是类，而是对类的一组描述，这些类要遵从接口描述的统一格式进行定义

4、接口不是类，不能使用new 实例化一个接口

5、可以声明接口的变量，但必须引用实现了接口类的对象

6、可以使用instanceof检查一个对象是否实现了某个特定的接口

7、有些接口只定义了常量，任何实现接口的类都自动继承了这些常量

8、接口里所有方法都是抽象方法，java8允许定义默认方法

9、不能包含构造器

10、只能包含静态常量，抽象方法，默认方法，类方法

11、接口里定义多个类共同的行为规范，访问权限都是public，可以忽略访问修饰符